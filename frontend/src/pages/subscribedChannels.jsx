import React from 'react'

const subscribedChannels = () => {
  return (
    <div>subscribedChannels
        const res = await API.get(`/subscription/u/${userId}`);



    </div>
  )
}

export default subscribedChannels